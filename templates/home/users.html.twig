<ul class="list-group list-group-flush">
    {% for user in users %}
        <a href="/profile?id={{ user.id }}" class="list-group-item list-group-item-action">{{ user.username }}</a>
    {% endfor %}
</ul>